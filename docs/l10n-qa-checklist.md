# 多语言收尾走查清单

> 每批完成后先跑脚本与单测；本文是**全部批次结束后**的真机验收清单。
> 设备：`4b0cd2e3`（adb）；真 APK：`D:\ai-build\rhodesisland\app-build\outputs\apk\debug\app-debug.apk`。

## 0. 构建与自动化

- [ ] `.\gradlew.bat :app:testDebugUnitTest --console=plain` → BUILD SUCCESSFUL
- [ ] `powershell -File tools\l10n-coverage.ps1` → 词典覆盖度 ≥ 95%，且「已包装但缺词条」= 0
- [ ] `L10nTest`：en/ja 词典 key 集合一致、无重复 key、无空译文、无跨批次译文冲突
- [ ] `.\gradlew.bat :app:assembleDebug` → 真 APK 时间戳更新（勿用 `app\build\outputs\` 下的陈旧残留）
- [ ] `adb install -r <真 APK>`（保留数据）

## 1. 语言切换（设置 → 语言）

- [ ] 四选项（跟随系统 / 简体中文 / English / 日本語）都能选中并打勾
- [ ] 切换后**当前页面立即**变文案（不重启、不重建 Activity、不丢滚动位置/输入草稿）
- [ ] 摘要行「当前：X」跟随更新
- [ ] 杀进程重进 → 语言保持
- [ ] 「跟随系统」：系统切中文 → 中文；切英文 → 英文；切日文 → 日文（系统语言变更会重建 Activity）
- [ ] 非中/英/日的系统语言 → 回退英文

## 2. 逐屏走查（三语言各走一遍）

| 页面 | 检查点 |
| --- | --- |
| 卡片流 / 角色页 | 标题、副标题、空态、按钮、搜索框占位符 |
| 聊天页 | 气泡内文案、操作栏（复制/朗读/删除/折叠）、空态、导出弹窗、Toast |
| 设置页 | 每个分区标题 + 摘要 + 行标题/副标题 + 开关文案；LLM API 区；本地 AI 引擎区；后端设置页 |
| 使用指南 | 分类卡、话题页、TIP/WARN、水平 chip、搜索占位符 |
| 群聊 | 列表、建群/群资料弹窗、@选择器、空态 |
| 世界书 | 列表、详情、条目编辑、作用域选择器 |
| 朋友圈 | 列表、发布弹窗、评论/点赞、设置区 |
| 视频 / 模型管理 | 卡片、进度、错误提示、模型下载页 |
| 小说 / 好感度 / 音乐 | 各页标题、按钮、弹窗 |
| 导出 | 导出格式选择、进度、完成/失败 Toast |

- [ ] 无「中英日混杂」残句（说明漏包装或词典缺词）
- [ ] 无 `{0}`/`{1}` 占位符漏出
- [ ] 长英文/日文文案不截断错行、不溢出（英文比中文长，重点看按钮和标签）
- [ ] 数字/日期/时间格式正常

## 3. Worker 与通知（界面语言抽查）

- [ ] 角色问候通知：标题/内容跟随界面语言（`L10nRuntime`）
- [ ] 群聊通知、朋友圈通知、前台服务常驻通知文案跟随语言
- [ ] 切换语言后**等一次后台触发**（或手动触发）再确认，避免只看到旧文案
- [ ] 通知渠道名（系统设置里显示）—— 渠道名只在创建时写入，老设备上保持旧语言属预期

## 4. 独立性验证（界面语言 ≠ AI 输出语言）

- [ ] 界面切 English / 日本語，角色回复仍是中文（提示词/人设未翻译）
- [ ] 日语翻译功能（聊天页）仍正常
- [ ] 指南搜索：中文关键词可搜；英文关键词搜不到中文标题属**已知限制**（已在 `docs/l10n-batches.md` 记录）

## 5. 回归

- [ ] 871+ 单测全绿
- [ ] 朋友圈生图开关、测试连接轮换、Token 用量等既有功能不回归
- [ ] 冷启动无闪退（OPPO/ColorOS 等国产 ROM 重点）
