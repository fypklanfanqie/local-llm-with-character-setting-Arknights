<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- 音频相关 -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- 读取媒体文件（Android 13+ 细分权限） -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- Android 12 及以下兼容 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- 角色问候：主动消息通知（Android 13+ 需运行时申请 POST_NOTIFICATIONS）-->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- 前台服务：本地推理保活 + 问候 Worker 前台化（Android 14 需声明类型与对应权限） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- WakeLock：推理/问候生成期间保持 CPU 唤醒 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- 电池优化白名单：请求忽略电池优化以保活后台问候 -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- 精确闹钟：问候 exact alarm 补时触发（Android 12+ 需用户授权，canScheduleExactAlarms 守卫） -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- 开机自启：重启后重新 arm 问候 alarm（WorkManager 自带 boot 恢复，此处补 alarm） -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".RhodesApp"
        android:allowBackup="true"
        android:extractNativeLibs="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RhodesIslandTerminal"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.RhodesIslandTerminal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 本地推理保活前台服务（dataSync：生成期间防进程被冻结/杀死） -->
        <service
            android:name=".service.InferenceForegroundService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!-- WorkManager 前台服务：问候 Worker setForeground 用，Android 14 需 app 显式声明类型 -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <!-- 问候 exact alarm 接收器（explicit PendingIntent 触发，无需 intent-filter） -->
        <receiver
            android:name=".work.GreetingAlarmReceiver"
            android:exported="false" />

        <!-- 开机后重新 arm 问候 alarm -->
        <receiver
            android:name=".work.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
